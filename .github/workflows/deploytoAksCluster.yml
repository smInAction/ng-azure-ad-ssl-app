on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: smregistry1.azurecr.io
        username: ${{ secrets.acr_smregistry1_username }}
        password: ${{ secrets.acr_smregistry1_password }}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t smregistry1.azurecr.io/smk8scluster2:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push smregistry1.azurecr.io/smk8scluster2:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.aks_smk8scluster2_kubeConfig }}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep ingress-basic2 | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace ingress-basic2`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: ingress-basic2
        container-registry-url: smregistry1.azurecr.io
        container-registry-username: ${{ secrets.acr_smregistry1_username }}
        container-registry-password: ${{ secrets.acr_smregistry1_password }}
        secret-name: smk8sclusterdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: ingress-basic2
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          smregistry1.azurecr.io/smk8scluster2:${{ github.sha }}
        imagepullsecrets: |
          smk8sclusterdockerauth
